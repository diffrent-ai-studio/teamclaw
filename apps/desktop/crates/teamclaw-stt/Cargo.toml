[package]
name = "teamclaw-stt"
version = "0.1.0"
edition = "2021"

[features]
default = []
whisper = ["dep:whisper-rs"]

[dependencies]
cpal = "0.17"
whisper-rs = { version = "0.15", optional = true }
anyhow = "1"
tokio = { version = "1", features = ["sync"] }
tracing = "0.1"
serde_json = "1"
